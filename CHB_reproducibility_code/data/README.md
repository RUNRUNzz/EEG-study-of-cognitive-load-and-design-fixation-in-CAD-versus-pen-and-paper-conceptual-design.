# Derived data for public repository

This folder is intentionally empty in the code-only package.

Before public release, place only de-identified derived data here. Use neutral participant IDs such as `S01` to `S28` consistently across files. Do not include a public mapping back to the original study IDs.

Required filenames and columns are listed in the repository-level `README.md`.
